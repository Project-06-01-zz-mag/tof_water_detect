#include "SimpleMath.h"

float add(float a, float b) {
        return a + b;
}

float sub(float a, float b) {
        return a - b;
}

float mul(float a, float b) {
        return a * b;
}

float div(float a, float b) {
        return a / b;
}

float SimpleMath::add() {
        return a + b;
}

float SimpleMath::sub() {
        return a - b;
}

float SimpleMath::mul() {
        return a * b;
}

float SimpleMath::div() {
        return a / b;
}